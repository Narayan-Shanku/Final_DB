@page
@model TeamAlpha.Pages.Vendors.CreateModel

@{
    ViewData["Title"] = "Create Vendor";
}

<div class="container py-5">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary mb-0">Add New Vendor</h1>
        <a asp-page="./Index" class="btn btn-outline-secondary rounded-pill px-4 shadow-sm">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>
    </div>

    <!-- Card Layout -->
    <div class="card shadow-lg border-0 rounded-4 p-4 p-md-5">
        <form method="post" class="needs-validation" novalidate>
            <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>

            <div class="row g-4">
                <!-- Name -->
                <div class="col-md-6">
                    <label asp-for="Vendor.Name" class="form-label fw-semibold">Vendor Name <span class="text-danger">*</span></label>
                    <input asp-for="Vendor.Name" class="form-control form-control-lg" placeholder="Enter vendor name" />
                    <span asp-validation-for="Vendor.Name" class="text-danger small"></span>
                </div>

                <!-- Email -->
                <div class="col-md-6">
                    <label asp-for="Vendor.Email" class="form-label fw-semibold">Email</label>
                    <input asp-for="Vendor.Email" type="email" class="form-control form-control-lg" placeholder="vendor@example.com" />
                    <span asp-validation-for="Vendor.Email" class="text-danger small"></span>
                </div>

                <!-- Phone -->
                <div class="col-md-6">
                    <label asp-for="Vendor.Phone" class="form-label fw-semibold">Phone Number</label>
                    <input asp-for="Vendor.Phone" class="form-control form-control-lg" placeholder="+1 (000) 000-0000" />
                    <span asp-validation-for="Vendor.Phone" class="text-danger small"></span>
                </div>

                <!-- Service Type -->
                <div class="col-md-6">
                    <label asp-for="Vendor.ServiceType" class="form-label fw-semibold">Service Type</label>
                    <input asp-for="Vendor.ServiceType" class="form-control form-control-lg" placeholder="e.g., Plumbing, HVAC, Electrical" />
                    <span asp-validation-for="Vendor.ServiceType" class="text-danger small"></span>
                </div>

                <!-- Location -->
                <div class="col-md-6">
                    <label asp-for="Vendor.Location" class="form-label fw-semibold">Location</label>
                    <input asp-for="Vendor.Location" class="form-control form-control-lg" placeholder="City / State" />
                    <span asp-validation-for="Vendor.Location" class="text-danger small"></span>
                </div>

                <!-- Rating -->
                <div class="col-md-6">
                    <label asp-for="Vendor.Rating" class="form-label fw-semibold">Rating</label>
                    <input asp-for="Vendor.Rating" class="form-control form-control-lg" placeholder="e.g., 4.5" />
                    <span asp-validation-for="Vendor.Rating" class="text-danger small"></span>
                </div>
            </div>

            <!-- Buttons -->
            <div class="text-center mt-5">
                <button type="submit" class="btn btn-primary btn-lg px-5 shadow-sm rounded-pill">
                    <i class="bi bi-building-add me-2"></i>Create Vendor
                </button>
                <a asp-page="./Index" class="btn btn-outline-secondary btn-lg ms-3 rounded-pill">
                    <i class="bi bi-x-circle me-1"></i>Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<!-- ✅ STYLE COPIED FROM ARCONE / INQUIRY / PROJECT / CONTRACTOR THEME -->
<style>
    /* Unified ArcOne Form Theme */
    body {
        background: linear-gradient(135deg, #f8fafc, #eef1f5);
    }

    .card {
        background: #ffffff;
        border-radius: 1.25rem;
        transition: all 0.3s ease;
    }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.08);
        }

    label.form-label {
        color: #2b2d42;
    }

    input.form-control, select.form-select, textarea.form-control {
        border-radius: 0.75rem;
        border: 1px solid #dee2e6;
        padding: 0.8rem 1rem;
        font-size: 1rem;
        transition: border-color 0.3s, box-shadow 0.3s;
    }

    input:focus, select:focus, textarea:focus {
        border-color: #0d6efd;
        box-shadow: 0 0 0 0.15rem rgba(13,110,253,0.25);
    }

    .btn-primary {
        background: linear-gradient(90deg, #0d6efd, #5a8dee);
        border: none;
        border-radius: 50px;
        transition: background 0.3s ease;
    }

        .btn-primary:hover {
            background: linear-gradient(90deg, #0056d2, #0d6efd);
        }

    .btn-outline-secondary {
        border-radius: 50px;
    }
</style>
